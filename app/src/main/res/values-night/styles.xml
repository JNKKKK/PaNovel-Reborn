<resources>

    <!-- No night-specific style overrides: AppTheme.PopupOverlay (values/styles.xml) is now
         DayNight-aware via colorSurface/colorOnSurface, so it needs no night variant. -->

</resources>
